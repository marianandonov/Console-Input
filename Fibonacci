using System;
using System.Collections.Generic;

namespace Fibonachi
{
    class Fibonacci
    {
        static void Main(string[] args)
        {

            ulong prevNum = 0, nextNum = 1, sum = 1;

            for(int i = 0; i < 100; i++)
            {
                sum = prevNum + nextNum;
                prevNum = nextNum;
                nextNum = sum;
                Console.WriteLine(nextNum);
            }

        }
    }
}
